find the second largest


Problem: Given a non-empty array of integers with at least two distinct values, return the second-largest unique value.

Input: array of integers nums
Output: integer — the second-largest distinct value
Example: [4, 1, 5, 5, 3] → 4
Time: O(n) — single pass keeping two best values.
Space: O(1)