count frequency of characters

Problem: Count the frequency of each character in a string and return a mapping (character → count).

Input: a string s
Output: mapping/dictionary from char → integer counts
Example: "abbc" → { 'a':1, 'b':2, 'c':1 }
Time: O(n)
Space: O(k) where k = number of distinct characters