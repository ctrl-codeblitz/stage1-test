#include <iostream>
#include <vector>
#include <string>
#include <sstream>

void solve() {
    // --- Input reading ---
    int a, b;
    std::cin >> a >> b;

    // --- Solution ---
    // The following variables are available:
    // a: int
    // b: int

    // TODO: Implement the solution
}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    solve();
    return 0;
}
